fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pypdf>=5.0
python-docx>=1.1
APScheduler>=3.10
python-multipart>=0.0.9
rapidfuzz>=3.9
jinja2>=3.1

# Opzionali: servono solo se si attiva il livello semantico dalle impostazioni.
# Basta quella del fornitore scelto. Senza, l'applicazione funziona ugualmente
# con il solo motore lessicale.
# google-genai>=1.0     # per Google Gemini (chiave gratuita)
# anthropic>=1.0        # per Anthropic Claude
